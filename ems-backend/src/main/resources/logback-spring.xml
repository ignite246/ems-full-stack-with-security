<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <property name="LOG_PATH" value="logs"/>
    <property name="LOG_FILE" value="employee-management.log"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/employee-management.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/employee-management-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d %-5level [%thread] %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
<!--

1. **`<configuration>`** – Root element that contains all Logback configurations.

2. **`<property>`** – Defines reusable variables such as log file name and log path.

3. **`<appender>`** – Specifies where logs are written (Console, File, Database, etc.).

4. **`ConsoleAppender`** – Displays logs on the application console.

5. **`RollingFileAppender`** – Writes logs to a file and automatically creates new log files when needed.

 6. **`<encoder>`** – Defines the format in which log messages are written.

7. **`<pattern>`** – Specifies the log layout (date, log level, class name, message, etc.).

8. **`<rollingPolicy>`** – Defines the rules for log file rotation (based on size and/or time).

9. **`<root level="INFO">`** – Sets the default logging level for the entire application.

10. **`<appender-ref>`** – Links the root logger to one or more appenders (e.g., Console and File).
-->